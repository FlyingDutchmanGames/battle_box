<div class="page-content container">
  <div class="subhead">
    <div class="subhead-heading">Lobbies / <%= @lobby.name %></div>
    <a href="<%= Routes.lobby_game_path(@conn, :index, @lobby.name) %>" class="btn">History</a>
  </div>

  <%= live_render(@conn, Scores, session: %{"lobby" => @lobby}) %>
</div>
