<div class="header">
  <a href="/"><div class="logo">Botskrieg</div></a>
  <%= if @current_user do %>
    <div class="nav-item">
      <a href="<%= Routes.live_path(@conn, BattleBoxWeb.Bots, @current_user.username) %>">Bots</a>
    </div>
    <div class="nav-item">
      <a href="<%= Routes.user_lobby_path(@conn, :index, @current_user.username) %>">Lobbies</a>
    </div>
    <div class="nav-item">
      <a href="<%= Routes.api_key_path(@conn, :index) %>">Keys</a>
    </div>
    <a href="<%= Routes.user_path(@conn, :show, @current_user.username) %>">
      <div class="user">
        <div class="name"><%= @current_user.username %></div>
        <img src="<%= @current_user.avatar_url %>">
      </div>
    </a>
    <div class="nav-item">
      <form id="logout" title="logout" action="<%= Routes.page_path(@conn, :logout) %>" method="post">
        <input name="_csrf_token" type="hidden" value="<%= Plug.CSRFProtection.get_csrf_token() %>">
        <input class="logout-button" type="submit" value="x">
      </form>
    </div>
  <% else %>
    <div class="github-login">
      <a href="<%= Routes.github_login_path(@conn, :github_login) %>">Login With Github</a>
    </div>
  <% end %>
</div>
